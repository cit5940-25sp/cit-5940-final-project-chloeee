package othello.gamelogic;

/**
 * Represents a human player in the Othello game.
 * A HumanPlayer does not use any automated strategy, and relies on user input to make moves.
 */

public class HumanPlayer extends Player {
    /**
     * Constructs a new HumanPlayer instance.
     * This player will manually select moves during their turn.
     */
    public HumanPlayer() {
    }
}
